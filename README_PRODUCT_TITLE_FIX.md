# STUDIO 82 — фикс дубля цвета в карточках

Что исправлено:
- раньше приложение собирало `title` как `model + color`, а ниже ещё раз выводило `color`;
- теперь название товара = только модель;
- цвет выводится отдельной строкой один раз;
- добавлена защита: если в базе у старого товара модель уже содержит цвет в конце, приложение уберёт этот цвет из отображаемого названия.

Проверка после деплоя:
- в каталоге должно быть: `Field General` и ниже `голубой`;
- не должно быть: `Field General голубой` и ниже `голубой`.
